# ---- Planejamento (mantido fora do versionamento pelo projeto) ----
specs
MASTER_PLAN.md

# ---- Segredos / configuração local ----
# Fase 7 (RF-06): appsettings.json foi higienizado — a connection string saiu para
# user-secrets (dev) / variáveis de ambiente (prod), então volta a ser versionável.
# Continuamos ignorando quaisquer overrides locais com segredo.
**/appsettings.*.Local.json
**/appsettings.Local.json
*.env
.env
.env.local
.env.*.local

# ---- .NET (backend) ----
[Bb]in/
[Oo]bj/
*.user
*.suo
.vs/
artifacts/

# ---- Node / Frontend (Vite) ----
node_modules/
dist/
dist-ssr/
.vite/
*.local
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# ---- SO / Editor ----
.DS_Store
Thumbs.db
.idea/
*.swp
